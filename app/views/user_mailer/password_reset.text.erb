Sample App

Hi, <%= @user.name %>

To reset your password click the link:
<%= edit_password_reset_url(@user.reset_token,
                                               email: @user.email) %>

This link expires in two hours.

If you don't request password reset please just ignore this message and your password stays as it is.